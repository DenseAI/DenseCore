apiVersion: 1

providers:
  - name: 'DenseCore'
    orgId: 1
    folder: 'DenseCore'
    folderUid: 'densecore'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/densecore
